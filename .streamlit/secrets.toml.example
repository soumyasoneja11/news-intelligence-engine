# Streamlit Community Cloud secrets (optional)
# Copy relevant keys to app settings → Secrets on share.streamlit.io

APP_PASSWORD = ""          # set to gate public access
ALLOW_REBUILD = "false"    # disable sidebar pipeline on cloud
TRENDING_REFERENCE = "auto"  # anchor trending on latest article date
